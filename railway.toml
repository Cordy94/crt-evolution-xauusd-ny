[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/home/trader/start.sh"
healthcheckPath = "/"
healthcheckTimeout = 60

[environment]
PORT = "8080"
NODE_ENV = "production"

# Recursos (gratis)
[resources]
cpu = 512
memory = 1024
